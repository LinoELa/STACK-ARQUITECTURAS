# Docker: NestJS

Guía rápida de Docker para una arquitectura backend con NestJS.

- Imagen base: node:20-alpine
- `npm run build` y `npm run start:prod`
- `docker-compose` con DB y app

Ver: BACKEND/arquitectura-nestjs.md
