# Docker: Flask

Guía rápida de Docker para una arquitectura backend con Flask.

- Imagen base: python:3.12-slim
- Comandos: `flask run` con `gunicorn`
- Entradas: `requirements.txt`, `app.py`

Ver: BACKEND/arquitectura-flask.md
