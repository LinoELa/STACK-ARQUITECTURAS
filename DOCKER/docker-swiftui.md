# Docker: SwiftUI

Guía rápida de Docker para entornos Swift/SwiftUI (macOS requiere build en CI con macOS).

- Imagen base: swift:latest para compilaciones server-side
- Para apps iOS usar Xcode en Mac

Ver: MOBILE/arquitectura-swiftui.md
